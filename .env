# LOCAL DEV ONLY (npm run dev). Everything runs on the local PHP server at :8000.
# Do NOT put the live spectranox.com URLs here — those belong in .env.production.
VITE_API_BASE=http://localhost:8000/backend/api
VITE_ADMIN_API_BASE=http://localhost:8000/admin-backend/api
VITE_SITE_URL=/
